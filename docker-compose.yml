version: "3.8"
services:
  api:
    build:
      context: ./apps/api
    ports:
      - "8081:8081"
    env_file:
      - .env
  web:
    build:
      context: ./apps/web
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - api